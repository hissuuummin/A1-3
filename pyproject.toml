[project]
name = "tripspark"
version = "0.1.0"
description = "TripSpark AI Travel Planner"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "api.generate:handler"
